const config = {
  API_BASE_URL:
    process.env.REACT_APP_API_URL || "https://asktopedia.onrender.com/api",
  SOCKET_URL:
    process.env.REACT_APP_SOCKET_URL || "https://asktopedia.onrender.com",
};

export default config;
